import { StickyBelowViewDirective } from './sticky-below-view.directive';

describe('StickyBelowViewDirective', () => {
  it('should create an instance', () => {
    const directive = new StickyBelowViewDirective();
    expect(directive).toBeTruthy();
  });
});
